# IntelPulse Evaluation & Trace

Generated by `backend/eval_and_trace.py`. Two live scans over the same input —
topic `Indian language AI models`, competitors `Sarvam AI`, `max_items=3`, `max_steps=8` —
one normal, one with the patent tool deliberately broken.

## Before / after

| Metric | Normal | chaos_mode=tool_failure |
| :--- | ---: | ---: |
| Latency (s) | 25.5 | 20.1 |
| Tool calls | 7 | 7 |
| Findings retrieved | 21 | 21 |
| Unrecovered errors | 0 | 0 |
| Recovered tier failures | 0 | 2 |
| Tools returning nothing | 0 | 0 |
| Total tokens | 0 (1 LLM calls failed) | 0 (1 LLM calls failed) |
| Analyst path | deterministic fallback | deterministic fallback |
| Patent records | 3 | 3 |
| Patent source served by | Google Patents WEB SEARCH fallback (NOT a verified patent database query) | Google Patents WEB SEARCH fallback (NOT a verified patent database query) |
| Task success | PASS | PASS |

## What failed and how it recovered

search_patents was forced to fail: the EPO OPS and Google Patents tier(s) raised ChaosToolFailure, the existing three-tier ladder fell through to Google Patents WEB SEARCH fallback (NOT a verified patent database query), which returned 3 real patent records, and the scan completed with 21 findings and 0 unrecovered errors.

## Reading the numbers

- **Unrecovered errors** counts tools that raised with no fallback left. It is the number that
  has to stay at 0 in both columns; a recovered tier failure is degradation, not failure.
- **Tools returning nothing** is not an error. A source with no matching results is reported as
  empty, and an unreachable source is reported as unavailable — the two are never conflated.
- Latency covers live third-party APIs and LLM synthesis, so it moves with the network. The
  chaos column is expected to be slower: it walks further down the patent ladder before it wins.
- Token counts come from Gemini's own `usage_metadata`, accumulated per run in `agent_brain`.
  A zero with failed calls means synthesis degraded to the deterministic analyst — which is why
  task success can still be PASS: an LLM outage costs narrative quality, not the findings.

> **Note:** the Gemini key hit its free-tier daily request quota (HTTP 429 `RESOURCE_EXHAUSTED`) during this run, so both scans synthesized with the deterministic analyst. Token instrumentation is live; there was simply no successful LLM call to count. Re-run once the quota resets for populated token counts.
